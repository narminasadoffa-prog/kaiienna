# 🚀 Запуск сайта локально

## Шаг 1: Проверьте Node.js

Откройте терминал (PowerShell или CMD) и выполните:
```bash
node --version
npm --version
```

Если команды не работают:
1. Скачайте Node.js: https://nodejs.org/
2. Установите LTS версию
3. Перезапустите терминал

## Шаг 2: Установите зависимости

В папке проекта выполните:
```bash
npm install
```

Это займет несколько минут.

## Шаг 3: Настройте переменные окружения

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
# Минимальная конфигурация для запуска
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="dev-secret-key-12345"
NODE_ENV="development"

# База данных (если есть PostgreSQL)
# DATABASE_URL="postgresql://user:password@localhost:5432/kaiienna?schema=public"
```

**Примечание:** Если у вас нет PostgreSQL, сайт все равно запустится, но некоторые функции (API, корзина) не будут работать.

## Шаг 4: Запустите проект

```bash
npm run dev
```

Дождитесь сообщения:
```
✓ Ready in X seconds
○ Local: http://localhost:3000
```

## Шаг 5: Откройте в браузере

Откройте: **http://localhost:3000**

---

## ⚠️ Если возникают ошибки:

### Ошибка: "Cannot find module"
```bash
# Удалите node_modules и установите заново
rm -r node_modules
rm package-lock.json
npm install
```

### Ошибка: "Port 3000 is already in use"
Измените порт в `package.json`:
```json
"dev": "next dev -p 3001"
```

### Ошибка с базой данных
Игнорируйте ошибки Prisma, если не используете БД. Фронтенд будет работать.

---

## 📝 Полезные команды:

```bash
npm run dev          # Запуск разработки
npm run build        # Сборка проекта
npm run start        # Запуск продакшена
npm run lint         # Проверка кода
```

---

## ✅ Готово!

Сайт должен открыться на http://localhost:3000

Если что-то не работает, проверьте:
1. Установлен ли Node.js
2. Установлены ли зависимости (`npm install`)
3. Создан ли файл `.env.local`
4. Нет ли ошибок в терминале



